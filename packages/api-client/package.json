{
  "name": "@rhasia-scret/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:full": "pnpm run lint && pnpm run typecheck && pnpm run test"
  },
  "dependencies": {
    "@rhasia-scret/api-contract": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "eslint": "^10.10.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
