# Supported PWA browser baseline

The PWA supports the current and previous major releases of Chrome, Edge, Firefox, and Safari that provide Web Crypto, IndexedDB, service workers, Clipboard API, and WebAuthn user verification. Private browsing and embedded webviews are unsupported for remembered unlock and offline storage because their persistence/security behavior is not reliable enough.
