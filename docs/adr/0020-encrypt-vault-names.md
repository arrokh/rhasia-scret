# Encrypt vault names

Vault names are encrypted vault content. The server retains only authorization and lifecycle metadata plus opaque encrypted blobs; after local unlock, the client decrypts names for navigation, while unopened vaults use a generic locked label.
