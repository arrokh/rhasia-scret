# Enrollment-gated shared-vault key delivery

> **Status:** superseded by ADR-0019

The MVP originally required the owner’s client to deliver a Key-Wrap Envelope after the recipient completed cryptographic enrollment. This was superseded to avoid requiring the owner to return before an invited user can access a Shared Vault.
