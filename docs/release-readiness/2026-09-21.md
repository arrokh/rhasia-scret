# Release readiness — 2026-09-21

## Decision: HOLD

The API architecture hardening is implemented locally. The change deepens authenticated application access, request-scoped API composition, passwordless identity lifecycle composition, and Application User Deletion completion without changing `/v1/**` contracts or client-only encrypted-content boundaries.

## Local evidence

- `mise exec -- pnpm run format:check` passed.
- `mise exec -- pnpm --filter @rhasia-scret/api lint` passed.
- `mise exec -- pnpm --filter @rhasia-scret/api typecheck` passed.
- `mise exec -- pnpm --filter @rhasia-scret/api test:unit` passed: 45 files, 287 tests.
- Final `mise exec -- pnpm run test:full` passed after the implementation and architecture documentation changes; API, web, client-core, contract, browser, mobile JavaScript, build, and release-evidence phases completed successfully.
- Vercel project filtering is configured and unit-tested: only `main` can deploy, and each project skips commits outside its app or transitive workspace dependencies.

## External blockers

Release remains **HOLD** because no new evidence is available for:

- production/provider deployment and custom API-domain cutover;
- live SMTP and database-backed deployment smoke checks;
- monitoring, alerting, retention scheduling, and rollback validation;
- credential rotation and legacy hosting retirement;
- real-device iOS/Android release checks.

No migration, deployment, credential rotation, or production cutover was performed. Repository Vercel configuration does not prove the external projects are linked to the intended `api-rhasia-scret` and `rhasia-scret` project names; verify those project settings and environment scopes manually. See [`api-architecture-deepening.md`](../api-architecture-deepening.md) for the implemented seams and [`2026-09-20.md`](2026-09-20.md) for the preceding release snapshot.
