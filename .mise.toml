[tools]
java = "21.0.2"
node = "24.19.0"

[tasks.setup]
description = "Install the pinned pnpm version and enable the repository Git hooks"
run = "npm install --global pnpm@11.17.0 && git config core.hooksPath .githooks"
